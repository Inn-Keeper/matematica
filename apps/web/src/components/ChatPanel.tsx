import type { Session } from "@supabase/supabase-js";

// ponytail: placeholder — replaced by the streaming chat panel in Task 9.
export function ChatPanel(_props: { month: string; session: Session }) {
  return null;
}
