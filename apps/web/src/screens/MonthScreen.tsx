import type { Session } from "@supabase/supabase-js";

// ponytail: placeholder — replaced by the real month screen in Task 8.
export function MonthScreen(_props: { session: Session }) {
  return null;
}
